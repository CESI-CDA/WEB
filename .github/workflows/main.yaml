name: Deploy React App

on:
  push:
    branches:
      - develop
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    runs-on: ubuntu-latest
    environment: Raspberry

    steps:
      # Étape 1 : Checkout du code source
      - name: Checkout repository
        uses: actions/checkout@v2

      # Étape 2 : Construction de l'image Docker
      - name: Build Dockerfile
        run: docker build -t cube-front .

      # Étape 3 : Sauvegarder l'image Docker en fichier tar
      - name: Save the Docker image as a tar file
        run: docker save -o cube-front.tar cube-front

      # Étape 4 : Afficher la liste des fichiers générés
      - name: List files
        run: ls

      # Étape 5 : Stocker l'image Docker en tant qu'artefact
      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-image
          path: cube-front.tar

  deploy-on-server:
    needs: [build]
    runs-on: ubuntu-latest
    environment: Raspberry

    steps:
      # Étape 6 : Télécharger l'image Docker depuis l'artefact
      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: build-image
          path: cube-front.tar

      # Étape 7 : Configuration de la connexion SSH avec clé privée
      - name: Setup SSH for connection
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      # Étape 8 : Transférer l'image Docker vers le serveur
      - name: Transfer Docker image via SCP
        uses: appleboy/scp-action@v0.1.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: cube-front.tar
          target: "/var/www/html/Cube-Front"

      # Étape 9 : Charger et démarrer le conteneur Docker sur le serveur
      - name: Run Docker container on server
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/html/Cube-Front/
            docker load -i cube-front.tar
            docker run -d --name cube-front-container -p 80:80 cube-front
